extends ../layout

block content
    h1.
         #{title}
    ul
        - each cabrequest, i in cabrequests
            li
                = cabrequest.name
                = cabrequest.phone
                = cabrequest.pickup
                = cabrequest.drop
                = cabrequest._id
                form(action='/cabrequests/#{cabrequest._id}/edit',method='post',enctype='application/x-www-form-urlencoded')
                    input(type='hidden',value='DELETE',name='_method')
                    button(type='submit').
                        Delete
                p 
                    a(href='/cabrequests/#{cabrequest._id}/edit') Edit
                p
                    a(href='/cabrequests/#{cabrequest._id}') Show